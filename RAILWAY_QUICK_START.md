# 🚂 Быстрый деплой на Railway

## 1. Создайте проект на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Нажмите **"New Project"**
3. Выберите **"Deploy from GitHub repo"**
4. Выберите ваш репозиторий

## 2. Добавьте PostgreSQL

1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст переменную `DATABASE_URL`

## 3. Настройте переменные окружения

В разделе **"Variables"** вашего backend сервиса добавьте:

### Обязательные переменные:

```
TELEGRAM_BOT_TOKEN=8246106554:AAGP9z5spKwGg40vgGHdC2vp7IkINKnthCQ
ADMIN_TELEGRAM_ID=5997725162
JWT_SECRET=your-super-secret-jwt-key-12345
```

### Опциональные переменные:

```
NODE_ENV=production
PORT=8000
WEBAPP_URL=https://your-webapp-url.railway.app
```

> 💡 **Важно:** Переменная `DATABASE_URL` создается автоматически при добавлении PostgreSQL

## 4. Деплой

1. Railway автоматически задеплоит ваше приложение после пуша в GitHub
2. Проверьте логи в разделе **"Deployments"**

## 5. Проверка работы

После успешного деплоя:

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Проверьте что бот отвечает

## Возможные проблемы

### Ошибка: "Missing required environment variables"

**Решение:** Убедитесь что добавили все обязательные переменные (см. шаг 3)

### Ошибка при миграциях Prisma

**Решение:** Проверьте что PostgreSQL база данных создана и переменная `DATABASE_URL` установлена

### Бот не отвечает

**Решение:** 
1. Проверьте логи в Railway
2. Убедитесь что `TELEGRAM_BOT_TOKEN` правильный
3. Проверьте что сервис запущен (статус "Active")

## Следующие шаги

1. **Настройте домен** (опционально)
   - В настройках сервиса → "Settings" → "Domains"
   
2. **Добавьте WebApp** (после деплоя frontend)
   - Обновите переменную `WEBAPP_URL`
   
3. **Настройте Google APIs** (опционально)
   - Добавьте переменные Google Calendar и Sheets

---

Готово! 🎉 Ваш бот работает на Railway.
