# Быстрый старт

Инструкция для быстрого запуска проекта локально.

## Шаг 1: Установка зависимостей

```bash
# Backend
cd backend
npm install

# WebApp
cd ../webapp
npm install
```

## Шаг 2: Настройка Telegram бота

1. Перейдите к [@BotFather](https://t.me/botfather)
2. Создайте бота командой `/newbot`
3. Скопируйте полученный токен
4. Получите ваш Telegram User ID через [@userinfobot](https://t.me/userinfobot)

## Шаг 3: Настройка Backend

```bash
cd backend

# Создайте .env файл
cp .env.example .env
```

Отредактируйте `.env` файл (минимальная конфигурация):

```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/tgbot_zapis"
TELEGRAM_BOT_TOKEN="ваш_токен_от_botfather"
ADMIN_TELEGRAM_ID="ваш_telegram_id"
WEBAPP_URL="http://localhost:3000"
JWT_SECRET="любая_случайная_строка"
PORT=8000
NODE_ENV=development
```

## Шаг 4: Настройка базы данных

Убедитесь, что PostgreSQL запущен, затем:

```bash
# Создайте базу данных
createdb tgbot_zapis

# Или через psql
psql -U postgres -c "CREATE DATABASE tgbot_zapis;"

# Примените миграции
npm run db:push

# Заполните тестовыми данными
npm run db:seed
```

## Шаг 5: Настройка WebApp

```bash
cd ../webapp

# Создайте .env.local файл
cp .env.example .env.local
```

Отредактируйте `.env.local`:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=ваш_bot_username
```

## Шаг 6: Запуск

Откройте два терминала:

```bash
# Терминал 1: Backend
cd backend
npm run dev

# Терминал 2: WebApp
cd webapp
npm run dev
```

Backend будет доступен на `http://localhost:8000`
WebApp будет доступен на `http://localhost:3000`

## Шаг 7: Настройка WebApp в Telegram

1. Перейдите к [@BotFather](https://t.me/botfather)
2. Выполните команду `/setmenubutton`
3. Выберите вашего бота
4. Введите текст кнопки: `Открыть приложение`
5. Введите URL: `http://localhost:3000`

Для тестирования на телефоне используйте [ngrok](https://ngrok.com):

```bash
# Установите ngrok
brew install ngrok  # macOS
# или скачайте с сайта

# Запустите туннель для WebApp
ngrok http 3000

# Используйте полученный https URL в BotFather
```

## Шаг 8: Тестирование

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Нажмите на кнопку меню внизу
4. Откроется WebApp приложение
5. Попробуйте создать запись!

## Проверка работы

### Backend
```bash
curl http://localhost:8000/api/health
# Должно вернуть: {"status":"ok","timestamp":"..."}
```

### Бот
Отправьте боту команду `/start` - должно прийти приветственное сообщение с кнопками.

### WebApp
Откройте `http://localhost:3000` в браузере - должна открыться главная страница.

## Частые проблемы

### База данных не подключается
```bash
# Проверьте, что PostgreSQL запущен
pg_isready

# Проверьте подключение
psql -U postgres -d tgbot_zapis
```

### Бот не отвечает
- Проверьте правильность `TELEGRAM_BOT_TOKEN` в `.env`
- Убедитесь, что Backend запущен
- Проверьте логи Backend на наличие ошибок

### WebApp не открывается в Telegram
- Убедитесь, что используете HTTPS URL (используйте ngrok)
- Проверьте правильность URL в BotFather
- Убедитесь, что WebApp запущен

## Следующие шаги

После успешного запуска:

1. Настройте Google интеграции (опционально, см. README.md)
2. Загрузите фотографии в галерею работ
3. Настройте услуги и цены через админ-панель
4. Настройте рабочие часы
5. Подготовьтесь к деплою на production (см. README.md)

## Деплой

Когда будете готовы к деплою на Railway, следуйте инструкциям в [README.md](./README.md#деплой-на-railway).

---

Удачи! 🚀
