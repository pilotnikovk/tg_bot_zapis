# Деплой на Railway - Пошаговая инструкция

## Шаг 1: Подготовка GitHub репозитория

### 1.1 Инициализируйте Git (если еще не сделали)

```bash
git init
git add .
git commit -m "Initial commit: Telegram WebApp для записи к мастеру"
```

### 1.2 Создайте репозиторий на GitHub

1. Перейдите на [github.com](https://github.com)
2. Нажмите **New repository**
3. Название: `tgbot-zapis` (или любое другое)
4. Оставьте **Private** (рекомендуется)
5. Нажмите **Create repository**

### 1.3 Подключите локальный репозиторий к GitHub

```bash
git remote add origin https://github.com/ваш-username/tgbot-zapis.git
git branch -M main
git push -u origin main
```

## Шаг 2: Создание проектов на Railway

### 2.1 Зарегистрируйтесь на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **Start a New Project**
3. Войдите через GitHub

### 2.2 Создайте проект

1. Нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Найдите и выберите `tgbot-zapis`
4. Railway автоматически обнаружит проект

## Шаг 3: Настройка Backend сервиса

### 3.1 Добавьте PostgreSQL

1. В проекте нажмите **+ New**
2. Выберите **Database** → **PostgreSQL**
3. Railway автоматически создаст БД и установит `DATABASE_URL`

### 3.2 Настройте Backend

1. Выберите сервис backend (он должен появиться автоматически)
2. Перейдите во вкладку **Variables**
3. Добавьте переменные окружения:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=8246106554:AAGP9z5spKwGg40vgGHdC2vp7IkINKnthCQ
ADMIN_TELEGRAM_ID=5997725162

# WebApp URL (будет обновлен позже)
WEBAPP_URL=https://ваш-домен.up.railway.app

# JWT Secret
JWT_SECRET=tgbot_zapis_jwt_secret_key_2024_super_secure_random_string

# Server
PORT=8000
NODE_ENV=production

# Google APIs (опционально, можно добавить потом)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REFRESH_TOKEN=
GOOGLE_SPREADSHEET_ID=
GOOGLE_CALENDAR_ID=
```

**ВАЖНО:** `DATABASE_URL` уже должен быть добавлен автоматически при создании PostgreSQL

### 3.3 Настройте деплой

1. Перейдите во вкладку **Settings**
2. В **Build Configuration**:
   - Root Directory: оставьте пустым `/`
   - Build Command: `npm run build`
   - Start Command: `npm start`
3. Нажмите **Deploy**

### 3.4 Получите URL Backend

1. После успешного деплоя перейдите во вкладку **Settings**
2. В разделе **Domains** нажмите **Generate Domain**
3. Скопируйте URL (например: `https://your-backend.up.railway.app`)

## Шаг 4: Настройка WebApp (Frontend)

### 4.1 Создайте новый сервис для WebApp

1. В проекте нажмите **+ New**
2. Выберите **GitHub Repo** → выберите тот же репозиторий
3. Railway создаст второй сервис

### 4.2 Настройте переменные окружения для WebApp

1. Выберите сервис webapp
2. Перейдите во вкладку **Variables**
3. Добавьте:

```env
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=sell_on_wb_bot
```

### 4.3 Настройте деплой WebApp

1. Перейдите во вкладку **Settings**
2. В **Build Configuration**:
   - Root Directory: `/webapp`
   - Install Command: `npm install`
   - Build Command: `npm run build`
   - Start Command: `npm start`

### 4.4 Получите URL WebApp

1. После деплоя перейдите во вкладку **Settings**
2. В разделе **Domains** нажмите **Generate Domain**
3. Скопируйте URL (например: `https://your-webapp.up.railway.app`)

## Шаг 5: Обновите настройки Backend

### 5.1 Обновите WEBAPP_URL

1. Вернитесь к сервису **backend**
2. Перейдите во вкладку **Variables**
3. Обновите `WEBAPP_URL` на URL вашего WebApp:
   ```
   WEBAPP_URL=https://your-webapp.up.railway.app
   ```
4. Backend автоматически перезапустится

## Шаг 6: Настройте Telegram бота

### 6.1 Обновите WebApp URL в BotFather

1. Откройте [@BotFather](https://t.me/botfather)
2. Выполните команду `/setmenubutton`
3. Выберите вашего бота `@sell_on_wb_bot`
4. Введите текст кнопки: `Открыть приложение`
5. Введите URL: `https://your-webapp.up.railway.app` (URL вашего WebApp)

### 6.2 (Опционально) Создайте нового бота

Если хотите создать отдельного бота для этого проекта:

1. Напишите [@BotFather](https://t.me/botfather)
2. Команда `/newbot`
3. Следуйте инструкциям
4. Скопируйте новый `TELEGRAM_BOT_TOKEN`
5. Обновите переменную в Railway Backend
6. Настройте `/setmenubutton` для нового бота

## Шаг 7: Проверка работы

### 7.1 Проверьте Backend

```bash
curl https://your-backend.up.railway.app/api/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### 7.2 Проверьте WebApp

Откройте `https://your-webapp.up.railway.app` в браузере - должна открыться главная страница

### 7.3 Проверьте бота

1. Найдите бота в Telegram
2. Отправьте `/start`
3. Нажмите на кнопку меню
4. Должно открыться WebApp приложение
5. Попробуйте создать запись!

## Шаг 8: Проверка базы данных

### 8.1 Подключитесь к БД через Railway

1. В Railway выберите сервис **PostgreSQL**
2. Перейдите во вкладку **Connect**
3. Скопируйте команду подключения или используйте встроенный SQL Editor

### 8.2 Проверьте данные

```sql
-- Проверить таблицы
SELECT tablename FROM pg_tables WHERE schemaname = 'public';

-- Проверить услуги
SELECT * FROM services;

-- Проверить пользователей
SELECT * FROM users;
```

## Автоматический деплой

Railway автоматически деплоит изменения при push в main ветку:

```bash
git add .
git commit -m "Update: добавил новую функцию"
git push origin main
```

Railway автоматически:
- Обнаружит изменения
- Запустит сборку
- Задеплоит новую версию

## Мониторинг

### Логи

В Railway перейдите в сервис → вкладка **Deployments** → **View Logs**

### Метрики

Вкладка **Metrics** показывает:
- CPU usage
- Memory usage
- Network traffic

## Стоимость

Railway предоставляет:
- **$5 бесплатно** каждый месяц
- **500 часов** выполнения бесплатно

Для этого проекта обычно достаточно бесплатного плана.

## Troubleshooting

### Backend не запускается

Проверьте логи:
```
Railway → Backend Service → Deployments → View Logs
```

Частые проблемы:
- ❌ Неправильный `DATABASE_URL` - проверьте, что PostgreSQL подключен
- ❌ Отсутствуют переменные - добавьте все необходимые env vars
- ❌ Ошибки миграции - проверьте логи Prisma

### WebApp не открывается в Telegram

- ✅ URL должен быть HTTPS
- ✅ URL должен быть настроен в BotFather через `/setmenubutton`
- ✅ `WEBAPP_URL` в Backend должен совпадать с реальным URL WebApp

### База данных пустая

Проверьте, что seed выполнился:
```bash
# В Railway CLI или через веб-интерфейс
cd backend && npx prisma db seed
```

## Полезные ссылки

- [Railway Documentation](https://docs.railway.app)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [Prisma с Railway](https://www.prisma.io/docs/guides/deployment/deployment-guides/deploying-to-railway)

---

**Поздравляем! 🎉** Ваш проект запущен в production на Railway!
