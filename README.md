# Telegram WebApp для записи к мастеру маникюра и педикюра 💅

Современное решение для автоматизации записи клиентов с интеграцией Google Sheets, Google Calendar и бонусной системой.

## Возможности

### Для клиентов
- 📅 Удобная онлайн-запись через Telegram WebApp
- 💅 Галерея работ мастера
- 🎁 Бонусная система (5% кэшбек + бонусы за отзывы)
- 🔔 Автоматические напоминания за 24ч и 2ч до визита
- ⭐ Система отзывов с фото
- 📱 История записей и бонусов в профиле
- 💬 Прямой чат с мастером через бота

### Для мастера
- ⚙️ Админ-панель для управления
- 📊 Статистика и аналитика
- 📋 Управление записями, услугами, расписанием
- 🖼️ Управление галереей работ
- 👥 База клиентов с историей
- 📊 Google Sheets синхронизация
- 📅 Google Calendar интеграция

## Технологический стек

### Backend
- Node.js + TypeScript
- Grammy (Telegram Bot Framework)
- Express (REST API)
- Prisma (ORM)
- PostgreSQL
- Google APIs (Sheets & Calendar)

### Frontend (WebApp)
- Next.js 14 (App Router)
- React + TypeScript
- Tailwind CSS
- React Query
- Telegram WebApp SDK

## Структура проекта

```
TGBot_zapis/
├── backend/                 # Backend + Telegram Bot
│   ├── src/
│   │   ├── bot/            # Telegram bot
│   │   ├── api/            # REST API
│   │   ├── integrations/   # Google APIs
│   │   ├── services/       # Business logic
│   │   └── database/       # Prisma client
│   └── prisma/
│       └── schema.prisma   # Database schema
├── webapp/                  # Next.js WebApp
│   └── src/
│       ├── app/            # Pages
│       ├── components/     # React components
│       └── lib/            # Utilities
└── README.md
```

## Установка и запуск

### Предварительные требования
- Node.js >= 18
- PostgreSQL
- Telegram Bot Token
- Google Cloud Project (для Sheets & Calendar)

### 1. Клонирование репозитория

```bash
git clone <repo-url>
cd TGBot_zapis
```

### 2. Настройка Backend

```bash
cd backend

# Установка зависимостей
npm install

# Копируем .env.example в .env
cp .env.example .env

# Редактируем .env файл
nano .env
```

#### Настройка .env файла

```env
# Database
DATABASE_URL="postgresql://user:password@localhost:5432/tgbot_zapis"

# Telegram Bot
TELEGRAM_BOT_TOKEN="your_bot_token_from_botfather"
ADMIN_TELEGRAM_ID="your_telegram_user_id"

# WebApp URL
WEBAPP_URL="http://localhost:3000"

# JWT Secret
JWT_SECRET="generate_random_string_here"

# Google APIs (опционально)
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
GOOGLE_REFRESH_TOKEN="your_google_refresh_token"
GOOGLE_SPREADSHEET_ID="your_spreadsheet_id"
GOOGLE_CALENDAR_ID="your_calendar_id"

# Server
PORT=8000
NODE_ENV=development
```

#### Инициализация базы данных

```bash
# Создать миграции
npm run prisma:migrate

# Заполнить тестовыми данными
npm run db:seed
```

#### Запуск Backend

```bash
npm run dev
```

Backend будет доступен по адресу `http://localhost:8000`

### 3. Настройка Frontend (WebApp)

```bash
cd ../webapp

# Установка зависимостей
npm install

# Копируем .env.example в .env.local
cp .env.example .env.local

# Редактируем .env.local
nano .env.local
```

#### Настройка .env.local

```env
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=your_bot_username
```

#### Запуск WebApp

```bash
npm run dev
```

WebApp будет доступен по адресу `http://localhost:3000`

### 4. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/botfather)
2. Получите токен и добавьте в `.env` файл Backend
3. Настройте WebApp через BotFather:
   ```
   /setmenubutton
   [Выберите вашего бота]
   [Введите текст кнопки: "Открыть приложение"]
   [Введите URL: http://localhost:3000 или ваш production URL]
   ```

### 5. Получение Telegram User ID (для админа)

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш User ID
3. Добавьте в `.env` как `ADMIN_TELEGRAM_ID`

## Настройка Google интеграций (опционально)

### Google Sheets

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com)
2. Включите Google Sheets API
3. Создайте OAuth 2.0 credentials
4. Получите refresh token (используйте OAuth Playground)
5. Создайте новую Google Таблицу
6. Скопируйте Spreadsheet ID из URL
7. Дайте доступ к таблице вашему Google аккаунту
8. Добавьте credentials в `.env`

### Google Calendar

1. В том же Google Cloud проекте включите Google Calendar API
2. Получите Calendar ID (обычно это ваш email или "primary")
3. Добавьте в `.env`

## Деплой на Railway

### 1. Подготовка

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Авторизуйтесь
railway login
```

### 2. Создание проекта

```bash
# Инициализация проекта
railway init

# Создание PostgreSQL базы
railway add -d postgres
```

### 3. Настройка переменных окружения

В Railway Dashboard добавьте все переменные из `.env` файла.

### 4. Деплой

```bash
# Деплой Backend
railway up

# Получите URL развернутого Backend
railway domain

# Обновите NEXT_PUBLIC_API_URL в настройках WebApp
```

### 5. Запуск миграций

```bash
railway run npm run prisma:migrate
railway run npm run db:seed
```

### 6. Настройка Telegram WebApp URL

Обновите WebApp URL в BotFather на production URL:
```
/setmenubutton
[Выберите вашего бота]
[Введите текст кнопки: "Открыть приложение"]
[Введите production URL]
```

## Использование

### Команды бота

- `/start` - Главное меню с кнопкой открытия приложения
- `/mybookings` - Открыть страницу с записями
- `/admin` - Админ-панель (только для администратора)
- `/help` - Справка

### API Endpoints

#### Публичные
- `GET /api/services/categories` - Категории с услугами
- `GET /api/services` - Все услуги
- `GET /api/health` - Проверка здоровья сервера

#### Защищенные (требуют Telegram auth)
- `GET /api/bookings/available-slots` - Доступные слоты
- `POST /api/bookings` - Создать запись
- `GET /api/bookings/my` - Мои записи
- `POST /api/bookings/:id/cancel` - Отменить запись
- `GET /api/user/me` - Данные пользователя
- `GET /api/gallery` - Галерея работ
- `GET /api/reviews` - Отзывы
- `POST /api/reviews` - Создать отзыв
- `GET /api/bonuses/history` - История бонусов

#### Админские
- `POST /api/admin/services` - Создать услугу
- `PUT /api/admin/services/:id` - Обновить услугу
- `DELETE /api/admin/services/:id` - Удалить услугу
- `GET /api/admin/bookings` - Все записи
- `GET /api/admin/stats` - Статистика

## Разработка

### Backend

```bash
cd backend

# Development mode с hot reload
npm run dev

# Build
npm run build

# Production
npm start

# Prisma Studio (DB GUI)
npm run prisma:studio
```

### Frontend

```bash
cd webapp

# Development
npm run dev

# Build
npm run build

# Production
npm start

# Lint
npm run lint
```

## Архитектура базы данных

### Основные таблицы

- **users** - Пользователи (клиенты)
- **masters** - Мастера
- **service_categories** - Категории услуг
- **services** - Услуги
- **bookings** - Записи
- **work_examples** - Примеры работ
- **reviews** - Отзывы
- **bonus_accounts** - Бонусные счета
- **bonus_transactions** - Транзакции бонусов
- **notifications** - История уведомлений
- **time_blocks** - Блокировка времени
- **settings** - Настройки системы

## Уникальные фишки

1. **Умная система напоминаний** - автоматические уведомления за 24ч и 2ч
2. **Бонусная система** - 5% кэшбек + бонусы за отзывы
3. **История красоты** - клиенты видят свои прошлые работы
4. **Умное расписание** - автоматический подбор свободного времени
5. **Быстрые действия** - повтор последней записи в 1 клик
6. **Чат с мастером** - прямое общение через бота
7. **Push-уведомления** - о новых работах, акциях, свободных окнах
8. **Отзывы с фото** - мотивация оставлять отзывы (бонусы)
9. **Статистика для мастера** - загруженность, популярные услуги, средний чек

## Будущие улучшения

- [ ] Интеграция онлайн-оплаты (ЮKassa/Stripe)
- [ ] Система лояльности с уровнями
- [ ] Реферальная программа
- [ ] SMS уведомления
- [ ] Аналитика посещаемости
- [ ] Автоматические акции
- [ ] Интеграция с Instagram
- [ ] Мобильное приложение (React Native)
- [ ] Система очередей для walk-in клиентов

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.

## Лицензия

MIT

---

Сделано с ❤️ и Claude Code
